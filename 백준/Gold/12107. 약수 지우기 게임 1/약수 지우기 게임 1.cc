#include <iostream>

using namespace std;

int main() {
    int N;
    cin >> N;

    if (N == 1) {
        cout << "B" << endl;
    } else {
        cout << "A" << endl;
    }

    return 0;
}